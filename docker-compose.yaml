version: '3.8'
services:
  app:
    image: "${DOCKERHUB_REPO}:${TAG}"
    ports:
      - "8080:8080"
  postgres:
    image: postgres:15
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
